# 归档模板

复制这个模板目录后，可以快速创建一个新的归档条目。

## 标准结构

- `request/`：保存需求原文和背景资料
- `code/`：保存该任务独立代码、脚本和配置
- `docs/`：保存实现说明、使用方法和交付结果
- `assets/`：保存截图、附件和相关文件

## 使用要求

每个新条目都应当自包含，不依赖仓库中的其他条目目录。

如果某段通用代码需要给当前任务使用，建议直接放到当前条目的 `code/` 中，而不是引用外部公共目录。